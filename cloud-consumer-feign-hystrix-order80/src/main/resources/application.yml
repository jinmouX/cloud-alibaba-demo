server:
  port: 80
spring:
  application:
    name: feign-hystrix-consumer-server
eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/

feign:
  hystrix:
    enabled: true